services:
  torgle:
    image: wadetyler/torgle-gateway:latest
    ports:
      - "9000:9000"
    environment:
      # Used to set default internal port for docker. (Not necessary if using port 9000)
      - "PORT=9000"
    volumes:
      - ./torgle-config.yml:/usr/src/data/torgle-config.yml
